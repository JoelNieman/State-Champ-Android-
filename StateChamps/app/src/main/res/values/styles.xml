<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/SCRedColor</item>
        <item name="colorPrimaryDark">@color/SCBlueColor</item>
        <item name="colorAccent">@color/SCGrayColor</item>
    </style>

</resources>
